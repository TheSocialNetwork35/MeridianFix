# MeridianFix modification, 2026-09-25: build-only workflow; no upstream publishing or issue writes.
name: Verify MeridianFix
on: [push, pull_request]
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'
      - uses: gradle/actions/setup-gradle@v4
      - run: ./gradlew --no-daemon build
      - uses: actions/upload-artifact@v4
        with:
          name: meridianfix-build-and-tests
          path: |
            build/libs/
            build/reports/tests/
